name: popperscuv2
description: App de ventas e inventario (poppersCUventas) con clientes, descuentos y reportes
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.5
  csv: ^5.0.2
  file_picker: ^8.3.2
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
